import { StyleSheet } from "react-native";

const SearchStyles = StyleSheet.create({
    contentContainer: {
        flexGrow: 1,
        padding: 5,
    },
    searchBarContainer: {
        flexDirection: 'row',
    },
})

export default SearchStyles;