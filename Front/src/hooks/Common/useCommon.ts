import React from "react";

const useCommon = () => {

    const decideBtn = (title: string) => {
        if (title === "수정") {

        }
    };
    
    return {
        decideBtn
    }
}

export default useCommon;